import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = { title: "Lead lists | Hubbly Outreach", description: "Review lead lists, address verification, and audiences ready for outreach." };
export default function Layout({ children }: { children: ReactNode }) { return children; }
